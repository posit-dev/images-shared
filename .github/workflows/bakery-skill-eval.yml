name: Bakery Skill Eval

on:
  pull_request:
    paths:
      - ".claude/skills/bakery/**"
  push:
    branches:
      - main
    paths:
      - ".claude/skills/bakery/**"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

jobs:
  eval:
    name: Eval
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2
        with:
          persist-credentials: false

      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b  # v8.1.0
        with:
          enable-cache: false

      - name: Install dependencies
        working-directory: .claude/skills/bakery/evals
        run: uv sync

      - name: Run evals
        working-directory: .claude/skills/bakery/evals
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: uv run inspect eval task.py --model anthropic/claude-sonnet-4-6

      - name: Upload eval logs
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: eval-logs
          path: .claude/skills/bakery/evals/logs/
          retention-days: 7
          if-no-files-found: ignore
