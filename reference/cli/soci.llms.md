# bakery soci

# bakery soci

Convert images to SOCI-enabled images

``` bash
bakery soci COMMAND [ARGS]...
```

    Usage: bakery soci [OPTIONS] COMMAND [ARGS]...

      Convert images to SOCI-enabled images

    Options:
      --help  Show this message and exit.

    Commands:
      convert  Convert images referenced by build-metadata JSON files into...

## Commands

`convert`  
[Convert images referenced by build-metadata JSON files into SOCI-enabled images.](../../reference/cli/soci/convert.llms.md)

Back to top
