package:
  ca-certificates:
    installed: true
  curl:
    installed: true
  git:
    installed: true
  cmake:
    installed: {{ if eq .Env.IMAGE_VARIANT "Minimal" }}false{{ else }}true{{ end }}

file:
  python-3.13.7:
    exists: {{ if eq .Env.IMAGE_VARIANT "Minimal" }}false{{ else }}true{{ end }}
    path: /opt/python/3.13.7
  r-4.5.1:
    exists: {{ if eq .Env.IMAGE_VARIANT "Minimal" }}false{{ else }}true{{ end }}
    path: /opt/R/4.5.1
  quarto-1.8.24:
    exists: {{ if eq .Env.IMAGE_VARIANT "Minimal" }}false{{ else }}true{{ end }}
    path: /opt/quarto/1.8.24
