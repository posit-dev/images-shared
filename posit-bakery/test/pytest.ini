[pytest]

markers =
    unit: Unit tests
    functional: Functional tests
    config: Configuration tests
    manifest: Manifest tests
    toml_schema: TOML schema tests

bdd_features_base_dir = features/
